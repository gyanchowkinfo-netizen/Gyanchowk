{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --prefix ../.. --include=dev && rm -rf node_modules && ln -sfn ../../node_modules node_modules",
  "buildCommand": "npm run build:web --prefix ../..",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://gyanchowk-1.onrender.com/api/:path*"
    }
  ]
}
